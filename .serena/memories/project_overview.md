# Project Overview: Padel Buddy Web

## Purpose
Padel match score tracker web application — a client-only SPA built with TanStack Start on Cloudflare Pages.

## Tech Stack
- React 19
- TanStack Start (SPA mode, `spa.enabled: true`)
- TanStack Router (file-based routing)
- Vite 7 + `@vitejs/plugin-react` + `vite-tsconfig-paths`
- TypeScript 5.9+ (strict mode)
- CSS Modules + CSS Variables for styling
- pnpm 10.27.0 as package manager
- Node 24.14.0 (pinned in `package.json engines` + `mise.toml`)
- Biome (linter/formatter — planned)
- Playwright (E2E tests — planned)
- Vitest (unit tests — planned)
- Cloudflare Pages (deployment)

## Directory Structure
- `src/routes/` — TanStack Start file-based routes (`__root.tsx`, `index.tsx`)
- `src/router.tsx` — Router configuration
- `src/routeTree.gen.ts` — Auto-generated by TanStack Router plugin
- `src/styles.css` — Global styles
- `.github/` — GitHub config (ISSUE_TEMPLATE, workflow docs)
- `.opencode/` — Agent skills, prompts
- `docs/` — PRD, plan files

## Important Notes
- ARCHITECTURE.md specifies `app/` directory, but implementation uses `src/`
- `complete-check` script is currently a no-op placeholder
- Path aliases: `#/*` → `./src/*` (both package.json imports and tsconfig), `@/*` → `./src/*` (tsconfig only)
